
/* ==========================
   README — How to run
   ========================== */
// FRONTEND
// place these files in a folder named `frontend/`:
// - index.html
// - styles.css
// - script.js
// then open `index.html` directly OR serve with any static server:
//   npx http-server

// BACKEND
// use the same FastAPI backend in the other canvas
//   cd backend
//   pip install -r requirements.txt
//   uvicorn main:app --reload --port 8000
